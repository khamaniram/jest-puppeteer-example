{
  "private": true,
  "scripts": {
    "test": "jest --colors --maxWorkers=2 --verbose"
  },
  "devDependencies": {
    "chalk": "^2.4.2",
    "jest": "^24.5.0",
    "prettier": "^1.16.4",
    "puppeteer": "^1.13.0",
    "rimraf": "^2.6.3"
  }
}
