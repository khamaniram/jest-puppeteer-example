{
  "private": true,
  "scripts": {
    "test": "jest --colors --maxWorkers=2 --verbose"
  },
  "devDependencies": {
    "chalk": "^2.4.2",
    "jest": "^24.5.0",
    "jest-html-reporters": "^1.1.3",
    "jest-mocha-reporter": "^1.0.1",
    "jest-spec-reporter": "^1.0.5",
    "jest-stare": "^1.13.1",
    "karma-spec-reporter": "0.0.32",
    "prettier": "^1.16.4",
    "puppeteer": "^1.13.0",
    "rimraf": "^2.6.3"
  }
}
